__pycache__/
.pytest_cache/
.freebuff/
*.db
*.sqlite3
.env
venv/
node_modules/

# Generated export artifacts from POST /schedule/export
dataset/uploads/

# Local secrets and provider config (see .env.example)
.env

# Large local backup archive (do not commit)
*.7z

# Added by code-review-graph
.code-review-graph/

# Derived embedding matrices: rebuilt from the baseline on demand,
# keyed by content hash, never a source of truth.
.cache/
.DS_Store
.venv/

# Scratch outputs from eval/benchmark runs. Every one is regenerable by the
# commands in METRICS.md section 6; none is a source of truth.
# tmp/pdfs/ is deliberately NOT ignored: it holds the rendered WSDOT IDR pages
# that back research/wsdot_c8078_verification_review.md.
tmp/*.txt
tmp/*.log
tmp/*.md
schedule_export_*.xml

# Deck build byproducts: regenerable from ppt_build/build_navis_sih_deck.mjs.
# The template inspection dumps, the superseded candidate decks and the codex
# finalizer staging area are not sources of truth.
ppt_build/template_inspection/
ppt_build/obsolete/
ppt_build/.codex-finalizer/
ppt_build/vitest-results.json
ppt_build/probe_api_output.txt
deliverables/*.inspect.ndjson
