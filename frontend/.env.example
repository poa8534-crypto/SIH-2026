# Optional. Leave unset and the app derives the API URL from the browser host,
# which is what makes the two-device LAN demo work without a rebuild.
# Only set this if the backend runs somewhere other than <same-host>:8000
# VITE_API_URL=http://192.168.1.42:8000
